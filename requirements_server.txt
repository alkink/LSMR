# Server requirements replicating Windows/WSL 'clrernet' environment
# Python: 3.10.18
# Install PyTorch first:
# pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 --index-url https://download.pytorch.org/whl/cu118

# OpenMMLab (Install openmim first, then use mim for these)
# pip install -U openmim==0.3.9
# mim install mmengine==0.8.4
# mim install mmcv==2.1.0
# mim install mmdet==3.3.0

# Standard Dependencies
numpy==1.24.3
scipy==1.15.3
h5py==3.15.1
ujson==5.11.0
opencv-python==4.12.0.88
opencv-python-headless==4.11.0.86
tqdm==4.65.2
tensorboard==2.20.0
thop

# Secondary dependencies from the environment
addict==2.4.0
albumentations==0.4.6
imageio==2.37.0
matplotlib==3.10.6
pandas==2.3.2
pillow==11.3.0
pyyaml==6.0.3
scikit-image==0.25.2
scikit-learn==1.7.2
shapely==2.1.2

# Mamba Requirements (Must be installed LAST without build isolation)
# pip install causal-conv1d==1.1.1 --no-build-isolation
# pip install mamba-ssm==1.1.1 --no-build-isolation
